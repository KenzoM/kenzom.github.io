let arr = [];
const Widget = function(picture,linkDemo,linkCode,projectTitle,projectDescription){
  this.picture = picture;
  this.linkDemo = linkDemo;
  this.linkCode = linkCode;
  this.projectTitle = projectTitle;
  this.projectDescription = projectDescription;
}
let ex = new Widget('picture url','linkDemo here','flfd','w/e title','whatever description');
arr.push(ex)

let kenzo = {name:"kenzo"}

module.export = kenzo;
